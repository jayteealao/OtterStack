services:
  web:
    image: nginx:alpine
    ports:
      - "8080:80"
    healthcheck:
      # This health check will always fail
      test: ["CMD", "sh", "-c", "exit 1"]
      interval: 5s
      timeout: 3s
      retries: 3
      start_period: 5s
